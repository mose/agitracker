.row
  .span16
    = link_to "<- List", admin_users_path, :class => "btn"
    - if can? :upate, User
      = link_to "Edit", edit_admin_user_path(@user.id), :class => "btn"
    - if can? :destroy, User
      = link_to "Delete", admin_user_path(@user.id), :controller => "admin/users", confirm: "Are you sure ?", method: :delete, :class => "btn pull-right"
.row
  %div
    %h3= @user.name
  %div
    = @user.email
