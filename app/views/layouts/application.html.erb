<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="utf-8">
  <title>Agitracker<%= ': ' + @title.capitalize unless @title.nil? %></title>
  <meta name="description" content="agitracker test application.">
  <meta name="author" content="Mose">

  <%= stylesheet_link_tag    :application %>
  <%= javascript_include_tag :application %>
  <%= csrf_meta_tags %>
</head>
<body>

<div class="container">
<%= render 'layouts/header', :page => @page %>

<section class="round">
<% flash.each do |key, value| %>
<div class="flash <%= key %>"><%= value %></div>
<% end %>

<%= yield %>

</section>

<%= render 'layouts/footer' %>
<%= debug(params) if Rails.env.development? %>
</div>

</body>
</html>
